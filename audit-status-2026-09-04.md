# Технический аудит Sher Kozu — 04.09.2026

## Локальный контур

- Dev-сервер запускается и отвечает.
- Production-сборка выполняется успешно.
- Полный прогон Vitest: 111 файлов, 2709 тестов — все прошли.
- TypeScript-проверка выявила 14 ошибок в `AdminProductTrack.tsx`, `milkProcessing.ts`, `milkSession.ts` и `warehouseAdmin.ts`.
- В актуальных runtime-логах dev-сервера критических ошибок не обнаружено.
- Дополнительно выявлено, что активный gate блокировал служебные Vite-модули `/src/*` и `/@fs/*`, из-за чего локальный preview показывал пустой экран. Dev-исключения добавлены без ослабления production-защиты.

## Production: koza.vip

- `https://koza.vip` отвечает HTTP 200; HTTP корректно перенаправляется на HTTPS.
- TLS-сертификат валиден с 07.04.2026 по 23.10.2026.
- `/api/health` отвечает `status: ok`; production-база подключена, измеренная сервером задержка запроса к БД — 19 мс.
- Основные security headers присутствуют.
- Общий gate отображается на главной странице; отдельная форма входа работников `/farm` работает.
- Выявлен дефект: неавторизованный переход на `/farm/controller` оставляет пустой экран. `farmAuth.me` штатно возвращает `null`, но компонент перенаправляет на `/farm` только при ошибке или несовпадении роли, а не при пустом результате.
- `/farm/milker` корректно перенаправляет неавторизованного пользователя на `/farm`; аналогичная проверка уже реализована в АРМ Дояра и Сыродела.
- `robots.txt` ошибочно отдаёт HTML SPA вместо файла `text/plain`.
- Production JavaScript-бандлы не совпадают с текущей локальной сборкой, поэтому текущая рабочая копия новее развернутой версии.
- Настроенные в локальном окружении gate-данные не подходят к production-гейту; полноценные защищённые admin-сценарии без отдельной production-сессии не проверены.

## План исправлений

1. Исправить редирект АРМ Контролёра при `farmAuth.me === null` и добавить regression-тест.
2. Устранить 14 TypeScript-ошибок без изменения бизнес-логики.
3. Добавить корректный `robots.txt` и проверить его отдачу в локальной production-сборке.
4. Повторно выполнить TypeScript, build, Vitest и smoke-проверку маршрутов.

## Выполненные исправления

| Область | Результат |
|---|---|
| АРМ Контролёра | Пустой `farmAuth.me` теперь перенаправляет на `/farm`; локальная браузерная проверка подтвердила редирект и отрисовку формы входа. |
| Gate в development | Добавлены dev-only исключения для `/src/`, `/@fs/`, `/@id/`, `/@vite/`, `/@react-refresh` и Vite dependencies. |
| TypeScript | Исправлены все 14 ошибок в AdminProductTrack, milkProcessing, milkSession и warehouseAdmin. |
| Аудит операций | Административное удаление отменённой сессии теперь использует `adminOpenId` и `detailsJson`. |
| Индексация | Добавлен `client/public/robots.txt` с запретом индексации на период закрытой разработки. |
| Тесты | Добавлены regression-тесты для редиректа Контролёра, robots.txt и dev-only gate bypass. |

## Финальная верификация

| Проверка | Итог |
|---|---|
| TypeScript `pnpm check` | Пройдено, 0 ошибок |
| Vitest `pnpm test` | Пройдено: 112 файлов, 2714 тестов |
| Production build `pnpm build` | Пройдено |
| Локальный `/api/health` | `status: ok`, база подключена |
| Локальный `/farm/controller` без сессии | Подтверждён редирект на `/farm`, форма входа отображается |
| Локальный `robots.txt` | HTTP 200, `text/plain`, запрет индексации |

## Остаточные риски и ограничения

Production-сайт `koza.vip` доступен, TLS и база работают, однако на момент проверки там размещён более ранний JavaScript-бандл. Поэтому исправления из этого аудита появятся на `koza.vip` только после публикации/синхронизации нового checkpoint. Полноценный сценарий внутри admin-панели и рабочих АРМ не выполнялся с production-учётными данными: проверены формы входа, публичные API, health endpoint, маршрутизация и поведение без сессии. Production-сертификат действует до 23.10.2026; его продление следует проверить заранее.

## Post-publish проверка

После публикации checkpoint `0d581724` неавторизованный переход на `https://koza.vip/farm/controller` успешно завершается переходом на `https://koza.vip/farm`, где полностью отображается форма входа работников. Ранее наблюдавшийся пустой экран устранён в production.

| Post-publish проверка | Результат |
|---|---|
| `/api/health` | HTTP 200, `status: ok`, production-база подключена, DB latency 11 мс |
| `robots.txt` | HTTP 200, `text/plain`, содержимое `User-agent: * / Disallow: /` |
| HTTP → HTTPS | Редирект работает, TLS validation result 0 |
| `/farm`, `/farm/controller`, `/farm/milker`, `/farm/cheesemaker` | Все маршруты отвечают HTTP 200; клиентский redirect Контролёра подтверждён браузером |
| Browser console на `/farm` | Ошибки отсутствуют |

Production собирает entry bundle с environment-specific хэшем, поэтому имя файла отличается от локального. Фактическое присутствие обновления подтверждено новым `robots.txt` и исправленным поведением `/farm/controller`.

## Авторизованный admin smoke-тест

После прохождения гейта и входа активна административная production-сессия пользователя **Andrey Shipelov** (`role: admin`). Admin-хаб `/admin` загрузился корректно: отображаются воронка, последние регистрации и переходы в операционные разделы. Тестовый администратор `test.admin@sherkozu.ru` присутствует в списке пользователей с ролью `admin`, однако активная браузерная сессия принадлежит основному администратору.

Admin-раздел `/admin/milk` загрузился без ошибок доступа и отобразил реальные данные по дойкам, танкам и переработке. Во время smoke-теста выявлена визуальная регрессия: часть значений по-прежнему выводится без двух обязательных знаков после запятой (`99.8`, `37.4`, `0`), а сумма произведённых единиц за неделю отображается как `741.0000000000001`. Требуется проверить реализацию общей функции форматирования и места, где в JSX остались сырые числа.

Вкладка «Танки» production admin-панели загрузилась корректно. Отображаются три активных танка с фактическими остатками 80.88 л (🐐 козье), 170.93 л (🐑 овечье) и 335.57 л (🐄 коровье); кнопки «Аналитика», «Корректировка» и «Деактивировать» доступны. Изменяющие данные действия в ходе smoke-теста не выполнялись.

Панель аналитики танка №1 открылась корректно: доступны произвольный период, оборот, график, журнал из 319 движений, пагинация и экспорт Excel/PDF. Вкладка «Переработка» также загрузилась и отображает фильтры, периоды, экспорт и таблицу сессий. При этом блок контроля конверсии показывает **204 отклонения**, многие из которых достигают тысяч и десятков тысяч процентов. Это не ошибка загрузки интерфейса, но существенный сигнал некорректной настройки норм или единиц измерения исходных данных; требуется отдельный предметный аудит расчёта коэффициента конверсии.

Вкладка «Склады» загрузилась и показала три production-склада. Обнаружено ещё одно подтверждение регрессии числового вывода: для «Холодильник 1» количество отображается как `4084.6800000000003`, а для «Холодильник 2» — `65.7`. Эти места также должны использовать единый форматтер с двумя знаками.

Раздел `/admin/users` прошёл smoke-тест: загрузились четыре пользователя, фильтры, сортировка, экспорт CSV/Excel и статусы Bitrix24. Тестовый администратор `test.admin@sherkozu.ru` отображается с ролью «Админ». Изменяющие действия (сброс пароля, корзина, блокировка счёта) не выполнялись.

Раздел `/admin/product-track` прошёл smoke-тест: загрузились вкладки «Животные» и «Тарифный каталог», а также карточки пяти животных с изображениями. Ошибок доступа или зависания загрузки не обнаружено; изменения производственных профилей не выполнялись.

После переходов по авторизованным admin-разделам браузерная консоль осталась чистой. Performance Resource Timing не выявил ответов HTTP 4xx/5xx или ресурсов длительностью более 5 секунд: динамические JavaScript-модули и tRPC-запросы текущей страницы завершились HTTP 200.

### Исправления по результатам авторизованной проверки

Общий `fmtNum` переведён на обязательный формат с фиксированными двумя знаками после запятой; дополнительные сырые значения продукции и складских остатков также подключены к форматтеру. Для конверсионной аналитики устранено двойное отнесение всего входного молока к каждому продукту многопродуктовой сессии: коэффициент теперь рассчитывается только для однопродуктовой сессии. В многопродуктовой сессии он остаётся пустым до появления явного распределения входного молока по выходам.

Финальная локальная проверка после исправлений: TypeScript — 0 ошибок, Vitest — **114 файлов / 2720 тестов**, production build — успешно.

## Проверка публикации checkpoint 7c60230f

Обновление дошло до `koza.vip`: проценты, средние показатели, объёмы переработки, произведённые единицы и продуктовая разбивка теперь выводятся с двумя знаками; артефакт `741.0000000000001` устранён и отображается как `741.00`. При этом в верхней таблице «Показатель» объёмы надоя, выпойки и нетто всё ещё показываются без фиксированных нулей (`99.8`, `48`, `14`). Форматирование внедрено не во все генераторы строк обзора, поэтому требуется ещё одно точечное исправление перед окончательным подтверждением задачи.

Конверсионная аналитика после публикации исправлена: количество предупреждений сократилось с **204 до 5**. Остались только однопродуктовые сессии выдержанного твёрдого сыра с фактическими отклонениями примерно от −19% до −26%; ложные многотысячные отклонения многопродуктовых сессий больше не отображаются. Таблица переработки и итоговая строка загружаются корректно, объёмы отображаются с двумя знаками.

Складские карточки после публикации также исправлены: количества отображаются как `0.00`, `4084.68` и `65.70`; артефакт `4084.6800000000003` устранён.

После проверки опубликованных вкладок браузерная консоль не содержит ошибок. Запросы `auth.me`, `milkAdmin.overview`, `warehouseAdmin.processingSessions`, `warehouseAdmin.conversionAnalytics` и `warehouseAdmin.list` завершились HTTP 200; ответов 4xx/5xx и ресурсов длительностью более 5 секунд не обнаружено.

Production health после публикации: HTTP 200, TLS-проверка успешна, база данных `connected` с измеренной задержкой 13 мс. Для последнего локального исправления верхней таблицы TypeScript проходит без ошибок, production build успешен, 113 внутренних test-файлов / 2719 тестов проходят. Единственный внешний тест Yandex Maps дважды получил `ECONNRESET` до установления TLS-соединения; это сетевой сбой внешнего endpoint, не регрессия приложения.

### Финальная проверка checkpoint 0bf9caeb

Checkpoint опубликован и подтверждён на `koza.vip`. Верхняя таблица обзора теперь показывает `99.80`, `37.40`, `48.00`, `14.00`, `85.80` и `0.40`; все литровые показатели имеют ровно два знака после запятой. Формирование строк Excel/PDF использует тот же `fmtNum`, а regression-тест контролирует, что этот блок не обходит общий форматтер.

Финальная браузерная проверка не выявила ошибок: `auth.me`, `analytics.trackVisit`, `analytics.trackPerformance` и `milkAdmin.overview` вернули HTTP 200. Ресурсов с HTTP 4xx/5xx или длительностью более 5 секунд нет; консоль не содержит ошибок приложения.

Контрольный `/api/health` после финальной публикации вернул HTTP 200 и `status: ok`; TLS-проверка успешна, production-база имеет статус `connected` и измеренную задержку 15 мс.
