# AdminClub Refactor Notes

## Текущее состояние

- UI-хелперы `MetricCard`, `EntityFormCard`, `EntityListCard`, `buildPaginationMeta`, `PaginationToolbar`, `FilterToolbar`, `PresetToolbar`, `SelectFilter`, `ListRow`, `Field` уже вынесены в `client/src/pages/adminClubUi.tsx`.
- `AdminClub.tsx` обновлён на импорт этих хелперов; текущая TypeScript-компиляция для client-side снова чистая.
- В `client/src/pages/adminClubSections.tsx` уже существует декомпозиция overview-секции.

## Точные границы вкладок в текущем AdminClub.tsx

- `posts`: строки 1263-1608
- `events`: строки 1609-1844
- `members`: строки 1845-2059
- `bitrix`: строки 2060-2642
- `activity`: строки 2643-3044

## Безопасная стратегия следующего шага

1. Оставить весь state, useMemo, useEffect, trpc queries/mutations и URL-sync в `AdminClub.tsx`.
2. Вынести каждую вкладку в отдельный компонент только как presentation + callbacks via props.
3. Сначала сформировать контракты props для `posts`, `events`, `members`, затем `bitrix` и `activity`.
4. После выноса прогнать vitest и status check.

## Известный внешний шум в логах

- В логах dev server остаётся старое сообщение про `server/db.ts:354` (`Expected ';' but found 'async'`), но текущий файл в этом месте синтаксически корректен.
- В консоли также есть не связанный напрямую с декомпозицией warning/ошибка: `[Database] Failed to upsert user: Error: No values to set`.
