# AdminClub remaining tabs snapshot

## Bitrix tab
- Detail payload block restored through selected lead payload and retry/snapshot history
- Audit trail block restored
- Remaining integration point spans the tail of the Bitrix section before `AdminClubBitrixTabSection` closes

## Activity tab
- Header actions restored: collapse, copy link, export scope, CSV, clear
- Preset buttons, sticky summary, type stats, area stats restored
- Filters restored: area/type
- Critical notification settings card restored
- Critical notification history restored
- Grouped session log and empty state restored

## Next step
Implement `AdminClubBitrixTabContent` and `AdminClubActivityTabContent` in `adminClubRemainingTabs.tsx`, then replace inline JSX in `AdminClub.tsx` with those components and rerun tests.
