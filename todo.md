# Project TODO

## Sprint 1 — Core Flow Foundation

- [x] Добавить enum-статусы животных, ownership и семьи в `drizzle/schema.ts`
- [x] Добавить таблицу `animals` в `drizzle/schema.ts`
- [x] Добавить таблицу `animal_media` в `drizzle/schema.ts`
- [x] Добавить таблицы `plans` и `plan_durations` в `drizzle/schema.ts`
- [x] Добавить таблицу `families` в `drizzle/schema.ts`
- [x] Добавить таблицу `animal_ownerships` в `drizzle/schema.ts`
- [x] Добавить foundation-таблицы `wallets` и `wallet_transactions` в `drizzle/schema.ts`
- [x] Применить схему через `pnpm db:push`
- [x] Добавить `listPublicAnimals()` в `server/db.ts`
- [x] Добавить `getAnimalBySlug()` в `server/db.ts`
- [x] Добавить `countActiveOwnerships()` в `server/db.ts`
- [x] Добавить `getAvailableSlotIndex()` в `server/db.ts`
- [x] Добавить `recalculateAnimalStatus()` в `server/db.ts`
- [x] Добавить helper для создания и обновления животных в `server/db.ts`
- [x] Добавить `animals.listPublic` в server router
- [x] Добавить `animals.getBySlug` в server router
- [x] Добавить `plans.listActive` или `plans.listForAnimal` в server router
- [x] Добавить `adminAnimals.list` в server router
- [x] Добавить `adminAnimals.create` в server router
- [x] Добавить `adminAnimals.update` в server router
- [x] Добавить `adminAnimals.setVisibility` в server router
- [x] Проверить защиту admin access через `adminProcedure`
- [x] Создать страницу `client/src/pages/AnimalsCatalog.tsx`
- [x] Создать страницу `client/src/pages/AnimalDetails.tsx`
- [x] Зарегистрировать маршруты `/animals` и `/animals/:slug` в `client/src/App.tsx`
- [x] Добавить CTA с `Home.tsx` на каталог животных
- [x] Показать в каталоге и карточке животного свободные слоты, species, показатели и стартовую цену
- [x] Добавить Vitest на расчёт доступных слотов
- [x] Добавить Vitest на `recalculateAnimalStatus()`
- [x] Добавить Vitest на admin access для CRUD животных
- [x] Добавить Vitest на `animals.getBySlug`
- [x] Запустить `pnpm test` и убедиться, что все тесты проходят
- [x] Проверить статус проекта после изменений
- [x] Повторно запустить полный `pnpm test` после продолжения Sprint 1 с admin UI
- [x] Повторно проверить статус проекта после Home CTA и admin UI
- [ ] Сохранить checkpoint после завершения Sprint 1
- [x] Исправить текущую JSX-ошибку в `client/src/pages/Home.tsx` перед стартом Sprint 1
- [x] Подтвердить устранение JSX-ошибки `Home.tsx` по актуальным логам dev server и сборке TypeScript
- [x] Добавить CTA и навигационный вход из `client/src/pages/Home.tsx` в каталог `/animals`
- [x] Создать первый admin UI для списка животных и базового управления каталогом
- [x] Зарегистрировать маршрут admin UI каталога животных в `client/src/App.tsx`
- [x] Добавить Vitest на новый UI/state helper, если будет вынесена бизнес-логика фильтрации или статусов
- [x] Добавить в admin UI форму создания животного
- [x] Добавить в admin UI форму редактирования существующего животного
- [x] Подключить форму к `adminAnimals.create` и `adminAnimals.update`
- [x] Добавить Vitest на helper-функции формы животных, если будут вынесены в отдельную логику
- [x] Добавить в форму животных загрузку изображений
- [x] Добавить список галереи текущего животного с превью
- [x] Подключить загрузку к `animalPhotos.upload`
- [x] Подключить получение галереи к `animalPhotos.list`
- [x] Добавить действия `setCover`, `reorderPhotos` и `deletePhoto` в UI галереи
- [x] Добавить Vitest на helper-функции галереи животных, если будут вынесены
- [x] Запустить полный `pnpm test` после добавления галереи животных в админскую форму
- [x] Добавить редактирование title и alt для каждого фото в галерее животного
- [x] Подключить сохранение метаданных фото к серверному API или подготовить совместимый flow
- [x] Добавить Vitest на helper-логику редактирования метаданных фото, если она будет вынесена
- [x] Проверить текущее состояние server/db.ts, server/routers.ts и admin UI галереи перед внедрением редактирования метаданных фото
- [x] Реализовать UI-поток сохранения title/alt у фото в `client/src/pages/admin/Animals.tsx` или фактическом admin-файле каталога
- [x] Подтвердить полный `pnpm test` и здоровье dev server после внедрения редактирования метаданных фото
- [x] Стабилизировать `client/src/pages/AdminClub.tsx` до корректного tRPC shape без TypeScript-ошибок
- [x] Стабилизировать `client/src/pages/ProductTracker.tsx` под фактический `productTracker` router без TypeScript-ошибок
- [x] Проверить, что проект снова компилируется без блокирующих ошибок перед дальнейшей доработкой галереи животных
- [x] Стабилизировать существующие Bitrix24 smoke-тесты после восстановления TypeScript-сборки
- [x] Проанализировать текущую реализацию кабинета Марты и определить полный перечень действий профиля животного для тиражирования
- [ ] Повторить логику всех действий профиля животного на остальных релевантных страницах и состояниях каталога
- [ ] Обновить Vitest-покрытие для повторённой логики профиля животного и подтвердить полный `pnpm test`
- [x] Унифицировать `AnimalProfile.tsx` под динамический slug вместо жёсткой Марты
- [x] Объединить `AnimalDetails.tsx` с универсальным динамическим профилем, чтобы не дублировать логику
- [ ] Унифицировать переходы из `Dashboard.tsx`, `ProductTracker.tsx` и `ClubFeed.tsx` на динамические профили животных
- [ ] Перенести все действия профиля животного (upload, remove, reorder, cover, share, lightbox, CTA-маршруты) на универсальный динамический профиль
- [ ] Заменить жёсткие ссылки `/animal/marta` и тексты Марты в `ProductTracker.tsx`, `ClubFeed.tsx`, `Dashboard.tsx` и связанных маршрутах на данные текущего животного
- [x] Актуализировать Vitest-покрытие для универсального профиля животного и связанных маршрутов после тиражирования логики
- [x] Перевести `/animals/:slug` на единый универсальный профиль с полной логикой кабинета Марты и убрать дублирующий упрощённый `AnimalDetails.tsx`
- [x] Свести `AnimalDetails.tsx` к переиспользованию универсального `AnimalProfile.tsx` без дублирования логики
- [x] Перевести маршруты `/animal/marta` и `/animals/:slug` на единый универсальный профиль в `App.tsx`
- [x] Заменить оставшиеся жёсткие переходы и тексты Марты в `Dashboard.tsx`, `ProductTracker.tsx` и `ClubFeed.tsx` на динамические ссылки текущего животного
- [x] Унифицировать `ProductTracker.tsx` на динамические CTA и ссылки профиля животного вместо жёстких переходов Марты
- [x] Унифицировать `Dashboard.tsx` на динамический переход к универсальному профилю животного вместо жёстких CTA Марты
- [x] Подтянуть динамические CTA и тексты животного в `Dashboard.tsx`, `ProductTracker.tsx` и `ClubFeed.tsx` через универсальные ссылки на профиль
- [x] Унифицировать `ClubFeed.tsx` на динамические CTA и ссылки профиля животного вместо жёстких переходов Марты
- [x] Унифицировать `client/src/components/Navbar.tsx` на динамический вход в актуальный профиль животного вместо жёсткой ссылки `/animal/marta`
- [x] Унифицировать CTA и маршруты в `client/src/pages/Home.tsx`, где ещё остались жёсткие ссылки `/animal/marta` и тексты про Марту
- [x] Обновить smoke-тесты после унификации Navbar и Home под динамический профиль животного
- [x] Повторно запустить полный `pnpm test` и проверить статус проекта после унификации Navbar и Home
- [x] Диагностировать и исправить недоступность маршрута `/admin/animals` для входа в админку животных
- [x] Обновить тесты на доступность `/admin/animals` и повторно подтвердить полный `pnpm test`
- [x] Повторно воспроизвести и устранить сохраняющуюся проблему доступа к `/admin/animals` после предыдущего исправления
